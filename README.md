# Void Fire — Minimal Blog

Open `index.html` in a browser to view the blog. To serve locally run:

```bash
python3 -m http.server 8000
# then open http://localhost:8000
```

The hero title uses large responsive typography (`WELCOME TO VOID FIRE`).
# Void-Fire